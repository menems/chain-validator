{
  "name": "chain-validator",
  "version": "0.0.1",
  "description": "ya chain wrapper for validator",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require should",
    "test-cov": "istanbul cover _mocha -- --require should --bail",
    "test-travis": "istanbul cover _mocha --report lcovonly -- --require should --bail"
  },
  "keywords": [
    "validation",
    "validator",
    "chain"
  ],
  "author": "blaz <blaz@menems.net>",
  "license": "MIT",
  "dependencies": {
    "validator": "^4.2.1"
  },
  "engines": {
    "node": ">= 4"
  }
}
